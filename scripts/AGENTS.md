# AGENTS.md

This directory contains repository maintenance and runtime build scripts.
Keep scripts explicit about required tools and environment variables; avoid hidden dependencies on local machine state.
