# AGENTS.md

`agl-config` loads and validates typed configuration for model rendering and local inference.
Config schemas use strict fields; do not add old-schema compatibility unless a stable public schema explicitly requires it.
