# AGENTS.md

`agl-config` loads and validates typed configuration for model rendering and local inference.
Config schemas use strict fields; do not accept older schema shapes unless a stable public schema explicitly requires it.
