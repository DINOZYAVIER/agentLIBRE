# AGENTS.md

`agl-config` loads and validates typed configuration for model rendering and local inference.
Config schemas use strict fields; preserve backward compatibility intentionally and test new fields.
