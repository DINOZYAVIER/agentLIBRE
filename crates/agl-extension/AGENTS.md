# AGENTS.md

`agl-extension` owns static extension declaration vocabulary and shared
extension-surface IDs. Keep it independent from CLI, runtime paths, turn FSM,
loop orchestration, model inference, and concrete extension implementations.
