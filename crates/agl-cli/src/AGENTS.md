# AGENTS.md

This directory contains CLI implementation code.
Do not put model rendering, backend execution, or agent policy logic here; use
`agl-oven`, `agl-inference`, and `agl-turn` for those responsibilities.
