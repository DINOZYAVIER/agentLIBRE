# System tests

Cross-product fixtures and acceptance artifacts live under this root. The
executable two-flow Agent/daemon/inference smoke is maintained at
`scripts/agl-daemon-live-smoke.sh` so it can be called directly by CI and
operator review commands.
