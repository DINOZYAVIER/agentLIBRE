# Build

For development builds, use `scripts/build-llama-cpp.sh` for llama.cpp and `cargo build -p agl-cli` for the CLI.

For local installation into `~/.cargo/bin`, use:

```sh
scripts/install-agl-cargo.sh
```
