# `agl trace`

Export deterministic semantic traces from the canonical redacted runtime event
log, or replay a trace without executing tools or external effects.

Content is excluded by default. Local content-addressed references require an
explicit metadata file during export.
