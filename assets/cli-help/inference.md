Low-level direct local inference commands.

Use these commands when intentionally bypassing agentFUNCTION defaults for
backend debugging, model smoke tests, or config repair.

Common use:
  agl inference run --config /path/to/local.toml --prompt "Reply once."
  agl inference chat --config /path/to/local.toml
