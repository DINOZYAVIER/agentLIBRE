# `agl trace replay`

Validate a semantic trace and compare its immutable AGL, Extension, Function,
Tool-set, policy, workflow, and inference identity with the expected identity.

Replay never invokes a Tool or executes an external effect.
