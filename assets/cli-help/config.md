Runtime configuration commands.

Use these commands to see where agl reads config from, check active runtime and
inference status, and write a starter runtime config. Local inference profiles
live under the config directory.
