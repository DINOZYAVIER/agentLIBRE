Run one prompt and print the final answer.

Use run for one-shot prompts. Add --function to load an agentFUNCTION, --skill
to include a core or trusted workspace skill, and --tool-mode to choose
filesystem access.

Common use:
  agl run --function coding --prompt "Summarize this workspace"
