Start a direct inference chat session.

This command does not load an agentFUNCTION, workspace default function, or
function evidence. Use top-level `agl chat` for the default function-backed
agent.

Common use:
  agl inference chat --config /path/to/local.toml
