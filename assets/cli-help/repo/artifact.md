Inspect and create declared .agl artifacts.

Artifact commands compare the current .agl folders with .agl/workspace.toml.
Use status to inspect, verify to fail on problems, sync to create missing
folders, and lock after accepted changes.
