Inspect and create declared workspace components.

Component commands compare the current .agl folders with .agl/workspace.toml.
Use status to inspect, verify to fail on problems, sync to create missing
folders, and lock after accepted changes.
