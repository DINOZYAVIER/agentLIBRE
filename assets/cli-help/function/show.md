Show one resolved agentFUNCTION.

The show command prints parsed metadata, the resolved system prompt path and
content, function-owned inference config when present, selected skills, and
declared subagents for a function id or explicit FUNCTION.md path.

Examples:
  agl function show coding
  agl function show .agl/functions/coding/FUNCTION.md --json
