Validate and print doctor hints for one agentFUNCTION.

Doctor runs the same static checks as status and prints the function smoke
prompt when one is configured. Use the printed run command to execute the smoke
prompt with the selected local model.

Examples:
  agl function doctor coding
  agl function doctor coding --json
