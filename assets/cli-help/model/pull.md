Download an exact Hugging Face GGUF into the standard cache and bind it.

Repository links show GGUF candidates only in an interactive terminal. In
automation, pass an exact blob or resolve file URL and --yes. Use --mmproj for
an explicit projector; neighboring files are never guessed. `--offline` and
standard `HF_HUB_OFFLINE=1` use complete cache entries only.
