Validate and bind an existing local GGUF without copying it.

Only the selected path is inspected. Use --mmproj to select a projector
explicitly and --replace to update a conflicting logical binding.
