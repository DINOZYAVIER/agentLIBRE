Terminate one live execution. Graceful termination is the default; use
`--immediate` for immediate tree termination. `--yes` confirms the mutation
without an interactive prompt. Detaching is not termination.
