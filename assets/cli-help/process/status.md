Show safe lifecycle metadata for one durable ExecutionId. Full argv,
environment values, input, output previews, raw process identifiers, and spool
paths are omitted. `--private-command` explicitly requests only the bounded
same-user display command; it is never part of safe model/runtime events.
