Replay bounded retained output strictly after a sequence cursor. UTF-8 and
base64 protocol chunks preserve exact bytes and stdout/stderr/terminal channel
identity. Retained output may expire after the configured terminal retention
period; expired output fails explicitly instead of appearing empty.
