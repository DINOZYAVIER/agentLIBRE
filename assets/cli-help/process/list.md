List process executions registered by the local daemon. The default includes
live executions only. Use `--all` for retained terminal records, `--session` or
`--run` to filter ownership, and `--json` for typed protocol fields.
