Manage canonical agent sessions through the agentLIBRE daemon.

These commands never create a private CLI session store. Session state,
transcripts, runs, cancellation, and completion remain daemon-owned.
