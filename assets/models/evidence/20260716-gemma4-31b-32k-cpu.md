# Gemma 4 31B 32K CPU admission evidence

Date: 2026-07-16

Historical note: this run used the former Unsloth alpha artifact and no longer
directly backs the canonical `gemma4-31b` package after AGL-153 switched it to
the official Google Q4_0 file. The 2026-07-23 combined record identifies the
replacement bytes and documents the conservative CPU-gate carry-forward.

The main GGUF SHA-256 used by these runs was checked as
`9188a71055550f1e60b875d02b7abb63625ac11b4a6f148d6b22b3b28ba3d335`.
Runs used the normal agentLIBRE function/session/model-manager path, CPU only,
Q8_0 K/V caches, context 32,768, batch 128, ubatch 64, and eight threads.

## Resource results

| Run | Limit | Output | Wall time | Maximum RSS | cgroup peak | Result |
| --- | ---: | --- | ---: | ---: | ---: | --- |
| `run_019f6e3d-a5b0-7f12-a744-30ad23033945` | unconstrained | `GEMMA4_31B_32K_PASS` | 52.76 s | 42,314,100 KiB | n/a | pass, zero reported swaps |
| `run_019f6e3e-c52b-7623-b451-58f913917033` | 24 GiB, no swap | none | 13.32 s | n/a | 24 GiB | OOM kill |
| `run_019f6e3f-2de4-7550-a9f3-66f7a256a032` | 36 GiB, no swap | `GEMMA4_31B_36G_NOSWAP_PASS` | 51.12 s | 48,154,768 KiB | 36 GiB | pass without reserve |
| `run_019f6e40-1c76-7350-abee-2348690a85e8` | 40 GiB, no swap | `GEMMA4_31B_40G_NOSWAP_PASS` | 48.70 s | 49,179,028 KiB | 34 GiB | pass with reserve |

GNU time's RSS includes file-backed mmap residency; cgroup results establish
admission. The former 24-GiB profile fails at 32K. Although one 36-GiB run
completed, it reached the limit exactly and provides no operating reserve. The
supported replacement therefore requires 40 GiB total RAM and 40,000,000,000
currently available bytes.

The runtime reported `n_ctx = 32768`, a 16,471.71-MiB mapped model buffer,
1,360-MiB non-SWA KV, 13,600-MiB SWA KV, and a 75.91-MiB compute buffer.
Initial no-skill prompts were 631-644 tokens and terminal output was clean.

## Tool and persisted-session results

Session `ses_019f6e40-0000-7000-8000-000000000031` passed all checks:

- `run_019f6e41-04bb-72c0-8fcc-af221aebebfd`: remembered
  `графитовый компас 631`;
- `run_019f6e42-1466-7711-a1df-e9bd4d444ec7`: emitted native `fs.read`,
  received a real observation, and returned both fixture lines verbatim; and
- `run_019f6e42-cac1-71f3-b405-e553cdde06ed`: after process restart, recalled
  the original phrase exactly.

The second tool inference used 717 cached tokens plus a 94-token observation
append. The resumed process rebuilt an 864-token prompt in the 32K context.

After the catalog gate and Hub-worker fixes were embedded into a fresh release
binary, `run_019f6e4b-2988-7b70-85e1-170d13c2c5e7` returned
`FINAL_31B_PROFILE_PASS` with `n_ctx = 32768` and a 627-token prompt.
