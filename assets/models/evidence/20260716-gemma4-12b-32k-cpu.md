# Gemma 4 12B 32K CPU admission evidence

Date: 2026-07-16

This record backs the `gemma4-12b` CPU profile in
`assets/models/catalog.toml`. The runs used the normal agentLIBRE
function/session/model-manager path, the in-process llama.cpp backend, CPU only,
Q8_0 K/V caches, a 32,768-token context, batch 128, ubatch 64, eight threads,
and the required projector.

Pinned artifacts were checked before inference:

- main: `cc9ff072e0a8203429ed854e6662c17a6c2bc1e5dca5b475dd4736caaacbc165`
- projector: `ecc4e93128da8363b7dbf2193eab98cf1142353f52ceaa0c95c0872997aaadd3`

## Resource results

| Run | Limit | Output | Wall time | Maximum RSS | cgroup peak | Result |
| --- | ---: | --- | ---: | ---: | ---: | --- |
| `run_019f6e0c-a664-7093-b055-58e358938482` | unconstrained | `GEMMA4_12B_32K_PASS` | 51.08 s | 19,249,392 KiB | n/a | pass, zero reported swaps |
| `run_019f6e12-5b95-77d0-8e91-74cfc90de929` | 12 GiB, no swap | none | 4.83 s | n/a | 12 GiB | OOM kill |
| `run_019f6e12-97c1-7760-a47e-022adae938df` | 16 GiB, no swap | `GEMMA4_12B_16G_NOSWAP_PASS` | 19.72 s | 19,414,960 KiB | 12.2 GiB | pass |

GNU time's RSS includes file-backed mmap residency and may exceed the cgroup
working set. The cgroup result is the admission boundary. The former 12-GiB
profile demonstrably fails at 32K; the replacement profile therefore requires
16 GiB total RAM and 14,000,000,000 currently available bytes.

The runtime reported `n_ctx = 32768`, a 6,390.13-MiB mapped model buffer,
272-MiB non-SWA KV, 5,440-MiB SWA KV, and a 76.16-MiB compute buffer. Initial
no-skill prompts were 631-646 tokens and terminal output contained only the
requested answers.

## Tool and persisted-session results

Session `ses_019f6e0d-0000-7000-8000-000000000012` passed all checks:

- `run_019f6e0f-8a84-7c42-a634-327b153f96c8`: remembered
  `серебряный маяк 812`;
- `run_019f6e10-0c25-7173-aeed-91a265503f58`: recalled it exactly;
- `run_019f6e10-941d-7c73-8c30-e01c9e4f01a3`: emitted native `fs.read`,
  received a real observation, and returned both fixture lines verbatim; and
- `run_019f6e11-3e24-7892-a9b4-a15067c3df92`: after a new process loaded the
  persisted transcript, recalled the same phrase exactly.

The tool result append used 764 cached tokens plus 94 new tokens. The resumed
process rebuilt a 907-token prompt inside the same 32K profile.

After the catalog gate and Hub-worker fixes were embedded into a fresh release
binary, `run_019f6e4a-72d4-76d1-978e-4efcc49961dc` returned
`FINAL_12B_PROFILE_PASS` with `n_ctx = 32768` and a 627-token prompt.
