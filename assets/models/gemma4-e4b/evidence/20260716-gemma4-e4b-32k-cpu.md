# Gemma 4 E4B QAT CPU 32K admission evidence

Date: 2026-07-16

This record backs the default `cpu-8gb-32768` profile in
`assets/models/catalog.toml`. The runs used the release agentLIBRE binary and
the normal function/session/model-manager path with the pinned E4B QAT Q4 main
model plus its required F16 projector. The runtime used `gpu_layers = 0`,
`context_tokens = 32768`, batch 128, ubatch 64, eight CPU threads, Q8_0 K/V
caches, mmap, and no swap in constrained cases.

The verified artifacts were unchanged from the AGL-144 acquisition evidence:

- main: `b3052f962d6449b4eb2075733c068bdec1c51eadb7b237e6c3157bfbb7b1dae0`
- projector: `6a255159ee4b01b304f633a57f017dd7d5a69d30fff52abb2614bf0813cef034`

## Results

| Limit | Output | Wall time | Maximum RSS | Result |
| --- | --- | ---: | ---: | --- |
| unconstrained | `AGL147_32K_RSS_PASS` | 7.36 s | 8,793,292 KiB | pass |
| 8 GiB, no swap | `AGL147_32K_8G_PASS` | 7.39 s | 8,793,984 KiB | pass |
| 6 GiB, no swap | `AGL147_32K_6G_PASS` | 7.39 s | 8,793,804 KiB | pass |

The constrained cases used user-systemd scopes with `MemoryMax=8G` and
`MemoryMax=6G`, respectively, plus `MemorySwapMax=0`. GNU time's RSS includes
file-backed mmap residency and can exceed the cgroup working-set limit; the
zero-exit cgroup result is the admission fact. Both constrained runs completed
without an OOM or swap, so the existing nominal 8-GB-class and 6,000,000,000
available-byte catalog gates remain conservative enough for this profile.

The native runtime reported two CPU KV buffers: 272 MiB non-SWA plus 680 MiB
SWA, both with 32,768 cells. The default no-skill prompt was 627-628 tokens.
Each terminal capture contained only the requested answer and timing output;
the projector tensor diagnostics were preserved in the attempt `runtime.log`
instead of being written to the terminal.

The isolated run roots were under `/tmp/agl-live-dogfood-20260716`. The source
revision was `2c8d2b293bbfe497d81daacdc747b83eef0f71d7` plus the recorded dirty
AGL-138 through AGL-147 implementation worktree.
