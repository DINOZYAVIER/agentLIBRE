# Gemma 4 26B-A4B 32K CPU admission evidence

Date: 2026-07-16

This record backs the `gemma4-26b` CPU profile in
`assets/models/catalog.toml`. The main GGUF SHA-256 was checked as
`dcf179a91153e3a7ece792e48ef872180d9d6ef9b7677f0a0bd3e83cfe624d5e`.
Runs used the normal agentLIBRE function/session/model-manager path, CPU only,
Q8_0 K/V caches, context 32,768, batch 128, ubatch 64, and eight threads.

## Resource results

| Run | Limit | Output | Wall time | Maximum RSS | cgroup peak | Result |
| --- | ---: | --- | ---: | ---: | ---: | --- |
| `run_019f6e1a-5f9d-70c3-933b-e1a947e51f54` | unconstrained | `GEMMA4_26B_32K_PASS` | 16.98 s | 31,277,136 KiB | n/a | pass, zero reported swaps |
| `run_019f6e1a-fe77-7841-92cc-fc8624bc6300` | 20 GiB, no swap | `GEMMA4_26B_20G_NOSWAP_PASS` | 16.45 s | 31,759,600 KiB | 17.3 GiB | pass |

GNU time's RSS includes file-backed mmap residency; the successful no-swap
cgroup run is the admission fact. The 20-GiB total class remains valid, while
the current-availability gate is raised to 20,000,000,000 bytes so the planner
does not admit a host below the measured 32K working set plus reserve.

The runtime reported `n_ctx = 32768`, a 13,573.86-MiB mapped model buffer,
340-MiB non-SWA KV, 3,400-MiB SWA KV, and a 67.27-MiB compute buffer. Initial
no-skill prompts were 635-649 tokens and terminal output was clean.

## Tool and persisted-session results

Session `ses_019f6e1b-0000-7000-8000-000000000026` passed all checks:

- `run_019f6e1b-74fc-7293-ab07-26d512d1a667`: remembered
  `медный горизонт 426`;
- `run_019f6e1b-f766-7401-b116-bf90dfac4a10`: emitted native `core.workspace:fs.read`,
  received a real observation, and returned both fixture lines verbatim; and
- `run_019f6e1c-c030-7231-8e27-570509232f00`: after process restart, recalled
  the original phrase exactly.

The second tool inference used 722 cached tokens plus a 94-token observation
append. The resumed process rebuilt an 869-token prompt in the 32K context.

After the catalog gate and Hub-worker fixes were embedded into a fresh release
binary, `run_019f6e4a-cc2f-7e33-b185-7283a2003940` returned
`FINAL_26B_PROFILE_PASS` with `n_ctx = 32768` and a 631-token prompt.
