# Gemma 4 31B 64K RX 7900 XTX admission evidence

Date: 2026-07-27

This record covers the exact automatic GPU tuple for the official
`gemma-4-31B_q4_0-it.gguf` artifact:

- 17,651,001,568 bytes;
- SHA-256
  `179cfb99212709597eae5929112cfca677e1bbf566178b479ae1da0c4772874b`;
- AMD Radeon RX 7900 XTX, PCI `1002:744c`, subsystem `1da2:471e`;
- 65,536 context tokens, batch 512, ubatch 256, eight threads;
- Q8_0 K/V, Flash Attention, `mmap = true`, unified KV, and no MTP; and
- all model layers on `Vulkan0`, with automatic fit disabled.

## Direct allocation diagnostic

The repository llama.cpp build loaded the exact artifact with `-c 65536`,
`-b 512`, `-ub 256`, `-ctk q8_0`, `-ctv q8_0`, `-dev Vulkan0`, `-ngl all`,
`-sm none`, and `-fit off`. It reported `n_ctx = 65536`, retained the requested
context without shrink, placed all layers on `Vulkan0`, generated one token,
and exited successfully.

## Reviewed production envelope

The initial production envelope retains the existing measured 31B model
allocation, expands the context allowance conservatively, and keeps the
existing uncertainty and desktop reserve:

| Component | Worker receipt ceiling MiB | Admission estimate MiB |
| --- | ---: | ---: |
| Model | 16,819 | 16,950 |
| Context | 3,850 | 3,850 |
| Transient | 320 | 320 |
| Uncertainty | — | 256 |
| Desktop reserve | — | 1,024 |

The resulting catalog gate is 22,400 MiB (23,488,102,400 bytes) on a
24,560-MiB device. Production promotion additionally requires an AGL release
worker run whose measured allocation receipt stays within every component of
this envelope.

## Installed worker verification

Release generation `generation-xziHOTaF` ran the exact builtin
`gemma4-31b` function through the isolated daemon. Run
`run_019fa703-cc28-7e63-bf00-7c563d63c072` recorded:

- `gpu_layers_requested = 999`, `selected_device = Vulkan0`, and
  `supports_gpu_offload = true`;
- all 61 model layers assigned to `Vulkan0`;
- `n_ctx = 65536`;
- a 16,818.21-MiB Vulkan model buffer;
- 2,720.00-MiB global plus 531.25-MiB SWA Vulkan KV buffers; and
- a 266.50-MiB Vulkan compute buffer.

The worker receipt was accepted without quarantine or cooldown. After the run,
the daemon released the idle model normally with zero unload failures.

The same turn completed ten separate `core.workspace:fs.read` calls and eleven
successful model attempts before the terminal `turn.finished` event with
`status = answered`. Its exact answer contained all ten fixture tokens. This
also verifies that the builtin function's 32-call budget is active beyond the
legacy eight-call boundary.
